## 2026-06-30
- **expected:** skill wyzwala się „po każdej zmianie hooków lub testów" i synchronizuje wg tabeli plików (zamiana ścieżek runtime `learning-loop`→`lore-keeper`).
- **actual:** ta sesja zmieniła treść 3 `SKILL.md` silnika (`reflect`/`skill-review`/`curator`) — dodanie inwariantu „Wykluczenie silnika", BEZ żadnej ścieżki runtime — oraz USUNĘŁA `reflect/FRICTION.md`. Trigger („Kiedy") nie obejmuje „zmiany treści skilla", a tabela wymienia tylko `reflect/SKILL.md` (i to wyłącznie dla ścieżki pending-reflect). Sync prozy skilla bez podmiany ścieżki ani usunięcie pliku nie były w zakresie → zrobione ręcznie, równoległymi edycjami, bez asysty skilla.
- **fix-hint:** rozszerzyć „Kiedy" na „po każdej zmianie hooków, testów LUB treści skilli (`SKILL.md`/`FRICTION.md`)"; dodać do procedury notę: większość zmian prozy skilla to identyczna kopia (zero podmian ścieżek — tylko `reflect/SKILL.md` niesie ścieżkę runtime); usunięcia plików (np. skasowany `FRICTION.md`) też trzeba odzwierciedlić w forku.
